export const GET = {
    HOMEPAGE: '/',
    ALL_USERS: '/users'
} as const;

export const POST = {
    SIGN_UP: '/sign-up',
    SIGN_IN: '/sign-in',
    SIGN_OUT: '/sign-out',
    ADD_ROUTE: ''
} as const;